\begin{table}[H]
    \centering
    \caption{Измерения параметров $RC$-цепи первого порядка (Задание 1)}
    \label{tab:rc1_data}
    \begin{tblr}{
        colspec = { X[c,m] X[c,m] X[c,m] X[c,m] },
        hlines, vlines,
    }
        \textit{Состояние ключа} & $u_{\text{вын}}$, В & $\tau$, мс \\
        Замкнут ($u_{1\text{вын}}, \tau_1$) & & \\
        Разомкнут ($u_{2\text{вын}}, \tau_2$) & & \\
    \end{tblr}
\end{table}

\begin{table}[H]
    \centering
    \caption{Измерения параметров $RLC$-цепи второго порядка (Задание 2)}
    \label{tab:rlc2_data}
    \begin{tblr}{
        colspec = { X[c,m] X[c,m] X[c,m] X[c,m] X[c,m] X[c,m] },
        hlines, vlines,
    }
        $u_{1\text{вын}}$, В & $u_{2\text{вын}}$, В & $U_{1m}$, В & $U_{2m}$, В & $t_1$, мс & $t_2$, мс \\
        & & & & & \\
    \end{tblr}
\end{table}

\begin{table}[H]
    \centering
    \caption{Измерения параметров $RLC$-цепи третьего порядка (Задание 4)}
    \label{tab:rlc3_data}
    \begin{tblr}{
        colspec = { X[c,m] X[c,m] },
        hlines, vlines,
    }
        $u_{2\text{нач}}$, В & $u_{2\text{вын}}$, В \\
        & \\
    \end{tblr}
\end{table}
