# Возможная проблема 1 (bib)
он может не подгружаться поскольку IF добавлены в etulab.cls -> packages.sty, typography.sty, commands.sty

# Matpllotlib -> pgf
Нужно сделать так, что какие бы пайтон скрипт не создавал pgfs, все они должны копироваться в pgfs/ корректно. Сейчас реализвано так, что один пайтон может создать один pgf, причем имена должны совпадать

# Обновление графиков в:
что это делает? почему так долго

# Он просто так на старте создает папку pgfs. ЗАчем?